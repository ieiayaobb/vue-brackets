import {
	CAPTURE
} from '../mutation-types'

import html2canvas from 'html2canvas'

const state = {

}

const getters = {

}

const mutations = {
	[CAPTURE] (state) {
		
	}
}

export default {
	state,
	getters,
	mutations
}